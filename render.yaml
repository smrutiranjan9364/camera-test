services:
  - type: web
    name: camera-test
    runtime: node
    branch: main
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: "20"
